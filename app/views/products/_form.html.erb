<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h1 class="title"><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h1>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand %>
  </div>

  <div class="field">
    <%= form.label :model %>
    <%= form.text_field :model %>
  </div>

  <div class="field">
    <%= form.label :variant %>
    <%= form.text_field :variant %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="field">
    <%= form.label :shortDescription %>
    <%= form.text_field :shortDescription %>
  </div>

  <div class="field">
    <%= form.label :longDescription %>
    <%= form.text_field :longDescription %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
