<p>The contents of your shopping cart are as follows:</p>

<ul>
<%shopping_cart_get_products().each do |k,v| %>
  <li><%=ProductType.find(k).event.name%>, <%=ProductType.find(k).event.start_date.in_time_zone()%>, <%=ProductType.find(k).product_zone.zone%>, <%=ProductType.find(k).price%>, <%=v%></li>
<%end%>
</ul>

<%= link_to 'Back', :back %>

<!-- TODO: remove items from cart -->
<!-- TODO: button for placing order -->